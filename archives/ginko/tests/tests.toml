[fibonacci]
file = "fib.gko"
expect = "55"
skip = false

[fibonacci with labels]
file = "fib-label.gko"
expect = "55"
skip = false

[spill registers]
file = "spill-reg.gko"
expect = "2825"
skip = false

[hello with putchar]
file = "hello.gko"
expect = "Hello!"
skip = false

[array fib in memory]
file = "fib-memory.gko"
expect = "[0, 1, 2, 4, 8, 16, 32, 64, 128, 256]"
skip = false