pow : (i32, i32) -> i32;
print_i32 : (i32) -> i32;

x: i32;

fun main() i32 {
	x = 10;
	print_i32(pow(2,x));
	0
};

fun pow(base: i32, exp: i32) i32 {
	var counter : i32 = 0;
	var res : i32 = 1;
	while counter < exp do {
		res *= base;
		counter += 1;
	};
	res
};

var x : i32;