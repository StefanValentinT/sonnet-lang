val print_i32 : (i32) -> struct{};

fun get_unit() {
	struct{}
}

fun main() {
	val unit = get_unit();
	print_i32(10);
	return unit;
}

# val main = fun () -> (true and true);

# val call_main = fun () -> main();

# fun factorial(x) {
# 	if x == 0i64
# 	then 1i64
# 	else x * factorial(x-1i64)
# }
