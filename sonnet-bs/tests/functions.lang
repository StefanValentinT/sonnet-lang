pow : (i32, i32) -> i32;
print_i32 : (i32) -> i32;

var power : i32 = 4;

fun main() i32 {
	print_i32(pow(2,power));
	0
};

fun pow(base: i32, exp: i32) i32 {
	var counter : i32 = 0;
	var res : i32 = 1;
	while counter < exp do {
		res *= base;
		counter += 1;
	};
	res
};

# expected stdout:
# 16