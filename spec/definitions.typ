#import "spec_template.typ": *

= Definitions

The following definitions apply to the specification. Other terms are defined when they first occur or in a more apporpriate section; when a term is defined it is being written in _italic_ type.

- *Behaviour* is all action that influences the world and is therefore observable. Observable influence may also be called physical.

- *Bit* is a unit of data storage capable of holding one of two values, commonly referred to as ${0, 1}.$ Individual bits do not need to be adressable.

- A *byte* is the smallest unit of bytes that is adressable.

- *Binary data* is the representation of data using bits, consisting of one or more bytes.

- A *character* is a member of a set of characters that make up the textual representation of a Sonnet program.